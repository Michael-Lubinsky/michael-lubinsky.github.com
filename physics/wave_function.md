

## Why I Suggested Schrödinger

The reason is mostly **pedagogical/visual pragmatism**, not physics preference:

The wavefunction ψ(x,t) is a **spatial field** — you can plot it, 
animate its time evolution inside a potential well, show |ψ|² as a probability density in real-time.  
Users can literally draw a potential V(x) with their mouse and watch the eigenstates appear.  
That maps naturally to an interactive UI.  

It's the more **visually obvious** choice. 

## What's Actually True Physically

The two formulations are **mathematically equivalent** — Dirac proved this.
They're related by a unitary transformation:

- **Schrödinger picture**: states |ψ(t)⟩ evolve, operators Ô are static
- **Heisenberg picture**: states are frozen, operators Ô(t) carry all time dependence, governed by the equation of motion: dÔ/dt = (i/ℏ)[H, Ô]

The choice is purely one of computational/conceptual convenience for a given problem.

## Where Heisenberg Matrix Mechanics Is Actually Superior

| Problem type | Better formulation | Why |
|---|---|---|
| Spin systems, qubits | Heisenberg | Hilbert space is **finite-dimensional** — matrices are exact, not discretized |
| Finite lattice models | Heisenberg | Tight-binding Hamiltonian *is* a matrix — exact diagonalization is natural |
| Quantum optics (Fock space) | Heisenberg | Ladder operators â, â† are matrices in truncated Fock space |
| Harmonic oscillator | Heisenberg | Analytic operator algebra is cleaner than solving the PDE |
| Continuous spatial potentials | Schrödinger | PDE on a grid, spatial intuition, easy to visualize |

Notice that my **tight-binding model explorer** idea (#2 in the list) is *already* Heisenberg matrix mechanics — you build H as a matrix and diagonalize it. I just didn't frame it that way.

## A More Honest Project Framing

A genuinely interesting project would be a **unified visualizer** that shows *both pictures simultaneously*:

- Left panel: wavefunction ψ(x,t) evolving in Schrödinger picture
- Right panel: the same physics shown as matrix elements ⟨m|Ô(t)|n⟩ evolving in Heisenberg picture
- Show explicitly how ⟨x(t)⟩ computed via Ehrenfest's theorem matches between both

This would be **more original** than yet another Schrödinger solver, 
and would address a real pedagogical gap — most students never viscerally see 
that the two pictures are the same physics. 
The matrix picture also connects directly to quantum computing 
(density matrices, Lindblad operators), making such a tool useful beyond just introductory QM.

The Schrödinger recommendation was the path of least resistance visually, not the most physically interesting or novel choice.


Yes — here is a practical design for an **Interactive Schrödinger Solver** that is realistic for a solo project and still useful to other people.

My recommendation is:

> Build **1D first**, with a modern browser UI, a Python solver core, and a clean path to 2D later.

That gives you something publishable fast, while keeping the architecture extensible.

## 1. Product scope

The tool should answer three kinds of user needs:

* **Learn**: see how a wavefunction evolves under a potential
* **Explore**: vary parameters and compare results
* **Compute**: get eigenstates, energies, observables, and exports

So the app should support two main modes:

* **Stationary mode**
  Solve `H ψ = E ψ` and show eigenvalues/eigenfunctions
* **Time-dependent mode**
  Solve `i ħ ∂ψ/∂t = H ψ` and animate `|ψ(x,t)|²`

For MVP, do only:

* 1D domain
* finite potential presets
* Gaussian packet initial state
* eigenstates + time evolution
* exports to PNG/CSV/JSON

---

## 2. Architecture

## Recommended overall architecture

### Frontend

* **React + TypeScript**
* plotting with **Plotly** or **ECharts**
* animation driven by precomputed frames or streamed snapshots
* Monaco editor or simple text area for custom potential formulas

### Backend

* **FastAPI** for API + typed contracts + autogenerated docs. FastAPI explicitly provides automatic interactive API docs and is built around Python type hints. ([FastAPI][1])
* Python numerical core using:

  * NumPy
  * SciPy sparse matrices / eigensolvers
  * optional QuTiP adapter later

### Storage

For MVP:

* local SQLite for simulation metadata
* result files on disk as JSON/NPZ/CSV

Later:

* Postgres + object storage

### Execution model

Use two execution paths:

#### Path A: local browser-only demo

* small 1D jobs run entirely in browser using Pyodide
* Pyodide supports NumPy and SciPy in the browser, which makes this viable for lightweight demos. ([Pyodide][2])

#### Path B: server compute

* heavier jobs sent to FastAPI backend
* backend computes and returns result bundle

This hybrid model is strong:

* users can try instantly
* serious runs still work
* same solver API can back both paths

---

## 3. High-level system diagram

```text
[React UI]
   |
   | REST / WebSocket
   v
[FastAPI API Layer]
   |
   +--> [Job Orchestrator]
   |         |
   |         +--> [Solver Core]
   |         +--> [Result Serializer]
   |
   +--> [Preset Library]
   +--> [Simulation Metadata DB]
   +--> [Artifact Storage]
```

For animation:

* small runs: return all frames at once
* medium runs: return decimated frames
* later: stream snapshots via WebSocket

---

## 4. Frontend design

The UI should feel like a lightweight scientific IDE.

## Main layout

### Left panel: problem setup

Tabs:

* Potential
* Initial state
* Domain/grid
* Solver settings
* Observables

### Center panel: plots

Stacked tabs:

* Probability density `|ψ|²`
* Real / imaginary parts
* Phase
* Potential overlay
* Eigenstate viewer
* Energy spectrum

### Right panel: results / run info

* normalized? yes/no
* grid size
* time step
* boundary conditions
* expectation values
* warnings

### Bottom panel: run history

* previous simulations
* duplicate / compare / export

## Nice UX details

* presets dropdown:

  * infinite well
  * finite well
  * harmonic oscillator
  * barrier
  * double well
  * periodic cosine potential
* sliders for common parameters
* “advanced mode” for formula input
* “compare runs” overlay

---

## 5. Solver approach: finite difference vs spectral

This is the most important design choice.

## My recommendation

For MVP:

> Use **finite difference** as the primary solver.

Then later add:

> **split-step spectral** for special time-dependent cases.

Why:

* finite difference is easier to explain
* works well with arbitrary 1D potentials
* handles stationary and time-dependent modes with one Hamiltonian construction model
* easier for custom boundaries and future 2D extension

---

## 6. Finite difference approach

For 1D Schrödinger equation:

```text
H = -(ħ² / 2m) d²/dx² + V(x)
```

Discretize space:

* grid `x_0 ... x_{N-1}`
* second derivative via tridiagonal stencil

Then:

* kinetic term becomes sparse tridiagonal matrix
* potential term becomes diagonal matrix
* full Hamiltonian is sparse Hermitian

This is perfect for sparse linear algebra.

SciPy’s `eigsh` is specifically for real symmetric / complex Hermitian sparse eigenproblems, and ARPACK is designed to find a few eigenpairs of large sparse operators efficiently. ([SciPy Documentation][3])

## Stationary solver with finite difference

Use:

* sparse Hamiltonian
* `eigsh(H, k=num_states, which="SM")` or shift-invert near target energy

Output:

* eigenvalues
* eigenvectors
* normalization checks

## Time-dependent solver with finite difference

Use one of these:

### Option 1: Crank–Nicolson

Best MVP choice.

Pros:

* stable
* norm-preserving in practice for Hermitian systems
* standard and reliable

Update:

```text
(I + iΔtH/2ħ) ψ_{n+1} = (I - iΔtH/2ħ) ψ_n
```

Because `H` is sparse, each step is solving a sparse linear system.

### Option 2: Matrix exponential / Krylov

Good later for performance and elegance.

### Option 3: Explicit RK

Not recommended as primary method for Schrödinger evolution.

Pros of finite difference:

* arbitrary `V(x)`
* simple sparse matrix structure
* one code path for eigen + evolution
* extendable to 2D

Cons:

* needs finer grids for high accuracy
* can be slower than spectral for smooth periodic cases

---

## 7. Spectral approach

There are two distinct “spectral” ideas people mix together:

### A. basis expansion in eigenfunctions

Expand wavefunction in some known basis.

Good when:

* harmonic oscillator
* infinite well
* analytically convenient systems

Bad for a general interactive solver:

* basis depends on problem
* harder for arbitrary user potentials

### B. split-step Fourier method

Excellent for time evolution when Hamiltonian separates nicely into kinetic + potential parts.

Typical update:

```text
ψ(t+Δt) ≈ e^(-iVΔt/2ħ) F^-1 e^(-iTΔt/ħ) F e^(-iVΔt/2ħ) ψ(t)
```

Pros:

* very fast for smooth problems
* elegant
* highly accurate for many wave-packet simulations

Cons:

* most natural with periodic boundary assumptions
* less intuitive for arbitrary boundary conditions
* not ideal as the only method for eigenstate solving

## Recommendation

Use spectral only as an optional “fast evolution engine” later:

* for free particle
* harmonic oscillator
* periodic / large smooth domains

---

## 8. Final solver strategy

### MVP

* **Finite difference**

  * stationary eigenproblem via sparse Hermitian eigensolver
  * time evolution via Crank–Nicolson

### V2

* add split-step Fourier mode for selected cases

### V3

* add 2D finite difference
* optional absorbing boundaries
* scattering workflows

---

## 9. Backend API design

Keep the API explicit and portable.

## Core endpoints

### Presets

```text
GET /api/presets
GET /api/presets/{id}
```

### Run stationary solve

```text
POST /api/solve/stationary
```

Request:

```json
{
  "grid": {
    "x_min": -10.0,
    "x_max": 10.0,
    "n_points": 800
  },
  "physics": {
    "mass": 1.0,
    "hbar": 1.0
  },
  "potential": {
    "type": "harmonic",
    "params": { "k": 1.0 }
  },
  "solver": {
    "num_states": 6
  }
}
```

### Run time evolution

```text
POST /api/solve/time-dependent
```

Request:

```json
{
  "grid": {...},
  "physics": {...},
  "potential": {...},
  "initial_state": {
    "type": "gaussian_packet",
    "params": {
      "x0": -4.0,
      "sigma": 0.8,
      "k0": 3.0
    }
  },
  "time": {
    "dt": 0.002,
    "steps": 2000,
    "save_every": 10
  },
  "solver": {
    "method": "crank_nicolson"
  }
}
```

### Fetch result

```text
GET /api/runs/{run_id}
GET /api/runs/{run_id}/frames
GET /api/runs/{run_id}/artifacts
```

### Compare runs

```text
POST /api/compare
```

---

## 10. Domain model

Use Pydantic models like:

* `GridConfig`
* `PhysicsConfig`
* `PotentialSpec`
* `InitialStateSpec`
* `TimeConfig`
* `SolverConfig`
* `RunSummary`
* `RunResultStationary`
* `RunResultTimeDependent`

This will keep frontend/backend contracts clean.

---

## 11. Suggested numerical core layout

Separate physics from transport/UI.

```text
solver_core/
  grid.py
  units.py
  potentials.py
  initial_states.py
  hamiltonian.py
  stationary.py
  time_evolution.py
  observables.py
  boundaries.py
  serialization.py
  validation.py
```

## Responsibilities

### `potentials.py`

Build `V(x)` from:

* preset
* formula
* piecewise definition

### `hamiltonian.py`

Build sparse matrix:

* kinetic operator
* potential diagonal
* total Hamiltonian

### `stationary.py`

* call `eigsh`
* normalize states
* compute expectation values

### `time_evolution.py`

* Crank–Nicolson stepper
* optional split-step later
* frame decimation

### `observables.py`

Compute:

* norm
* `<x>`
* `<p>`
* energy
* variance
* transmission/reflection estimates later

---

## 12. Repo structure

Here is a clean monorepo layout:

```text
interactive-schrodinger/
├── README.md
├── LICENSE
├── docs/
│   ├── architecture.md
│   ├── numerics.md
│   ├── api.md
│   └── physics-notes.md
├── apps/
│   ├── web/
│   │   ├── package.json
│   │   ├── src/
│   │   │   ├── app/
│   │   │   ├── components/
│   │   │   ├── features/
│   │   │   │   ├── presets/
│   │   │   │   ├── potential-editor/
│   │   │   │   ├── stationary-solver/
│   │   │   │   ├── time-evolution/
│   │   │   │   ├── run-history/
│   │   │   │   └── compare-runs/
│   │   │   ├── api/
│   │   │   ├── plots/
│   │   │   ├── state/
│   │   │   └── types/
│   │   └── public/
│   └── api/
│       ├── pyproject.toml
│       ├── app/
│       │   ├── main.py
│       │   ├── routes/
│       │   │   ├── presets.py
│       │   │   ├── stationary.py
│       │   │   ├── time_dependent.py
│       │   │   ├── runs.py
│       │   │   └── compare.py
│       │   ├── schemas/
│       │   ├── services/
│       │   ├── storage/
│       │   └── config.py
│       └── tests/
├── packages/
│   ├── solver-core/
│   │   ├── pyproject.toml
│   │   ├── solver_core/
│   │   │   ├── grid.py
│   │   │   ├── potentials.py
│   │   │   ├── initial_states.py
│   │   │   ├── hamiltonian.py
│   │   │   ├── stationary.py
│   │   │   ├── time_evolution.py
│   │   │   ├── observables.py
│   │   │   ├── io.py
│   │   │   └── validation.py
│   │   └── tests/
│   └── shared-contracts/
│       ├── json-schema/
│       └── examples/
├── examples/
│   ├── infinite_well.json
│   ├── harmonic_oscillator.json
│   ├── barrier_scattering.json
│   └── double_well.json
├── scripts/
│   ├── dev.sh
│   ├── seed_presets.py
│   └── benchmark_solver.py
└── .github/
    └── workflows/
```

## Why this structure works

* frontend stays independent
* solver core is reusable as a standalone Python package
* you can later publish `solver-core` to PyPI
* easier testing and benchmarking

---

## 13. MVP feature list

Here is the MVP I would actually build.

## Must-have physics features

* 1D stationary solver
* 1D time-dependent solver
* presets:

  * infinite well
  * finite well
  * harmonic oscillator
  * single barrier
  * double well
* Gaussian wave packet initial state
* normalized eigenstates
* probability density plot
* potential overlay
* animation controls

## Must-have numerical features

* sparse Hamiltonian construction
* Crank–Nicolson propagation
* eigenvalue solve for lowest `k` states
* validation:

  * norm drift warning
  * grid spacing warning
  * CFL-like guidance / quality hints

## Must-have UX features

* presets dropdown
* sliders for parameters
* run button
* pause / play animation
* inspect eigenstate index
* export:

  * PNG plot
  * CSV data
  * JSON run config

## Must-have engineering features

* unit tests for canonical systems
* benchmark script
* reproducible example files
* docs with equations and assumptions

---

## 14. First milestone plan

## Milestone 1: CLI and solver core

Before UI, build a CLI.

Example:

```bash
schrodinger solve stationary examples/harmonic_oscillator.json
schrodinger solve td examples/barrier_scattering.json
```

Why:

* easiest way to validate numerics
* reusable in CI
* easier debugging than starting with frontend

## Milestone 2: local web UI

* React app
* call backend API
* render plots and animation

## Milestone 3: browser-only mode

* Pyodide-backed demo for small runs
* no backend needed for toy examples

Pyodide is a good fit for this “demo mode” because it runs Python in the browser and supports scientific packages including NumPy and SciPy. ([Pyodide][2])

---

## 15. Validation strategy

You need trustable numerical tests.

## Canonical test cases

Use problems with known answers.

### Infinite square well

Check:

* eigenvalues scale as `n²`
* eigenfunctions match sine modes

### Harmonic oscillator

Check:

* `E_n = ħω(n + 1/2)` in chosen units

### Free Gaussian packet

Check:

* normalization remains close to 1
* packet center moves as expected

### Symmetric double well

Check:

* even/odd ground-state splitting

These tests matter more than fancy UI early on.

---

## 16. Where QuTiP fits

QuTiP already provides Schrödinger-equation solvers such as `sesolve` and broader time-evolution tooling. ([qutip.readthedocs.io][4])

I would not make QuTiP your core dependency for MVP, because:

* you want direct control of the discretized PDE solver
* your main differentiator is the interactive PDE/UI workflow
* finite-difference Hamiltonian construction is straightforward enough

But QuTiP is a great later integration:

* “advanced engine” option
* validation cross-checks
* open quantum systems extension later

---

## 17. Recommended tech stack summary

## Frontend

* React
* TypeScript
* Plotly
* Zustand or Redux Toolkit
* Tailwind

## Backend

* FastAPI
* Pydantic
* NumPy
* SciPy
* optional SQLAlchemy + SQLite

## Solver

* sparse finite difference
* `eigsh` for stationary
* Crank–Nicolson for time evolution

## Optional later

* Pyodide browser mode
* QuTiP integration
* WebSocket streaming
* 2D solver

---

## 18. My final recommendation

If you want the best solo-project path:

### Build this exact scope

* **1D only**
* **finite difference only**
* **stationary + time-dependent**
* **FastAPI + React**
* **CLI first, UI second**

That is small enough to finish, but substantial enough to be useful.

### Don’t do yet

* 3D
* symbolic formula parser with full generality
* many-body systems
* GPU acceleration
* full QuTiP replacement

Those can wait.

---

## 19. Best MVP tagline

You could position it as:

> **Interactive Schrödinger Studio**
> Explore 1D quantum systems with live potentials, eigenstates, and wave-packet dynamics.

That is understandable to students, researchers, and hiring managers.

I can next draft a **concrete API schema and folder-by-folder starter implementation plan** for this project.

[1]: https://fastapi.tiangolo.com/?utm_source=chatgpt.com "FastAPI"
[2]: https://pyodide.org/?utm_source=chatgpt.com "Pyodide — Version 0.29.3"
[3]: https://docs.scipy.org/doc/scipy/reference/generated/scipy.sparse.linalg.eigsh.html?utm_source=chatgpt.com "eigsh — SciPy v1.17.0 Manual"
[4]: https://qutip.readthedocs.io/en/latest/apidoc/solver.html?utm_source=chatgpt.com "Dynamics and Time-Evolution — QuTiP 5.2 Documentation"


## Some new features for this application

⏺ Here are feature ideas grouped by category, ordered roughly
  by value vs effort:

  ---
  High value, moderate effort

  1. Expectation values panel
  Show ⟨x⟩, ⟨x²⟩, ⟨p⟩, ⟨p²⟩, ⟨H⟩ as functions of time during
  evolution. Directly observable physics, easy to compute from
   existing psi_frames. ⟨p⟩ can be computed via finite
  differences on ψ.

  2. Probability current j(x,t)
  j = Im(ψ* ∂ψ/∂x). Animate alongside |ψ|² to show where
  probability is flowing. Very illuminating for tunneling and
  scattering cases.

  3. Momentum-space view
  Show |φ(k,t)|² = |FFT(ψ)|² alongside the position-space
  plot. Reveals wave-packet spreading in k-space,
  complementary to x-space view.

  4. Superposition of eigenstates as initial state
  Let the user pick coefficients c₁, c₂, c₃ for ψ₀ = Σ cₙψₙ.
  Right now only Gaussian initial states are supported. This
  would let users build Schrödinger cat states, observe beats,
   etc.

  ---
  High value, low effort

  5. Uncertainty principle display
  Compute Δx·Δp = √(⟨x²⟩-⟨x⟩²) · √(⟨p²⟩-⟨p⟩²) and show it
  alongside the Heisenberg bound ½. Directly connects to
  pedagogy.

  6. Download plot as PNG
  Plotly already has a built-in camera-icon toolbar button —
  just enable it. Zero backend work.

  7. Shareable URL for time-evolution params
  URL state is already implemented for stationary mode. Extend
   it to time-evolution params (x0, sigma, k0, dt, n_steps).

  ---
  Medium value, higher effort

  8. Side-by-side mode
  Show two runs simultaneously with different parameters (e.g.
   different σ or different potential) for direct visual
  comparison. Good for exploring how parameters affect
  dynamics.

  9. Custom potential drawing
  Let the user draw V(x) with a mouse on the plot canvas.
  Converts drawn curve to an array, bypasses the expression
  parser entirely. Very appealing for exploratory use.

  10. Finite square well: transmission/reflection coefficients
  For scattering potentials (step, barrier), compute T and R
  analytically from the numerical wavefunction at the
  boundaries after the packet has passed. Show alongside the
  animation.

  ---
  Lower priority / longer term

  11. 2D solver — already noted in CLAUDE.md as post-MVP. Grid
   abstraction is designed for this.

  12. Energy spectrum plot — for parametric exploration, plot
  E_n vs a parameter (e.g. well width L or barrier height V₀).
   Requires repeated solves but could be done with a sweep
  endpoint.

  13. Export animation as GIF/MP4 — useful for papers and
  presentations. Requires a server-side rendering step
  (matplotlib animation or similar).

  ---
  My top 3 recommendations for JOSS readiness

  1. Expectation values — makes the tool scientifically
  quantitative, not just visual
  2. Momentum-space view — standard physics pedagogy, very
  little backend work (just FFT)
  3. Superposition initial state — unlocks a much richer class
   of experiments



## Claude CLI - How To

Good timing to think about this before starting — how you communicate with Claude Code determines the quality of what gets built significantly more than the specific prompts.

## The Core Principle

Claude Code works best when it has **maximal context upfront and minimal ambiguity about decisions already made**. The enemy is Claude making architectural choices you didn't intend, which then get baked into 500 lines of code you have to undo.

---

## Step 1: Write a CLAUDE.md Before Anything Else

Create a `CLAUDE.md` file in your project root before the first session. Claude Code reads this automatically as persistent context. Put everything you've already decided in it:

```markdown
# Schrödinger Solver — Project Context

## What this is
A 1D quantum mechanics solver with browser UI.
Target: JOSS publication eventually.

## Decisions already made (do not revisit)
- Time evolution: Crank-Nicolson only for MVP
- Units: atomic units (ħ = m_e = 1)
- Boundary conditions: Dirichlet (ψ = 0 at walls) for MVP
- Grid abstraction must be dimension-agnostic (path to 2D)
- Backend: FastAPI + Python
- Frontend: React + TypeScript
- Animation: precomputed frames for MVP, SSE streaming later
- Expression parser: asteval or sympy restricted namespace — never eval()

## Validation requirements
Every solver must pass these before being considered done:
- Infinite square well: E_n = n²π²/2L² in atomic units
- Harmonic oscillator: E_n = n + 1/2
- Norm conservation: ||ψ(t)||² stays within 1e-6 of 1.0

## Primary user
Explorer — someone varying parameters and comparing results.
Not a pure tutorial tool.

## What NOT to build yet
- 2D solver
- SSE streaming
- Absorbing boundary conditions
- GPU acceleration
```

This file saves you from re-explaining context every session and stops Claude from "helpfully" making decisions you've already settled.

---

## Step 2: Use a Spec File Per Feature

Don't describe features in chat. Write a short spec file and tell Claude to implement it:

```
implement the spec in specs/01-grid-abstraction.md
```

The spec file might look like:

```markdown
# Grid Abstraction

## Goal
A Grid class that the Hamiltonian builder accepts.
Must work for 1D now, extensible to 2D without interface change.

## Interface
Grid(n_points, x_min, x_max) 
- .x  → ndarray of spatial coordinates
- .dx → grid spacing
- .n  → number of points
- .shape → tuple (n,) in 1D, (nx, ny) in 2D later

## Tests required
- Grid(100, -10, 10).dx == 20/99
- Grid(100, -10, 10).shape == (100,)
- x array is monotonically increasing

## Do not implement
- Any Hamiltonian logic in this file
```

This pattern — **spec file → implement → review** — is far more reliable than conversational back-and-forth.

---

## Step 3: Session Structure

Each Claude Code session should have a clear, single objective. Don't do this:

```
build the backend solver and also add the frontend and hook them up
```

Do this instead, across separate sessions:

```
Session 1: Grid + Hamiltonian builder + tests
Session 2: Crank-Nicolson stepper + tests  
Session 3: FastAPI endpoints + validation
Session 4: React scaffold + Plotly integration
Session 5: Wire frontend to backend
```

At the start of each session, Claude Code will re-read `CLAUDE.md` automatically. You can also say:

```
read CLAUDE.md and specs/02-hamiltonian.md before starting
```

---

## Step 4: Review Checkpoints — Don't Let It Run Too Long

Claude Code can generate a lot of code quickly. The danger is that wrong decisions compound. Use the `/review` pattern:

```
stop after implementing the Grid class. 
show me the interface before writing the Hamiltonian builder.
```

Then inspect and approve before continuing. For numerical code especially, **ask it to show you the tests before the implementation**:

```
write the tests for the Crank-Nicolson stepper first, 
then implement until the tests pass
```

Test-first is the single most effective pattern for getting correct numerical code from Claude Code.

---

## Step 5: Be Explicit About What "Done" Means

Claude Code will stop when it thinks something is done. Define done explicitly:

```
this task is complete when:
- pytest passes with no warnings
- the infinite square well ground state energy is within 0.1% of π²/2
- there are no bare except clauses
- no eval() or exec() anywhere in the codebase
```

---

## Practical Commands to Know

```bash
# Start a session with context
claude --context CLAUDE.md

# Ask it to only touch specific files
claude "implement the Grid class in src/core/grid.py only"

# Have it explain before doing (good for complex numerical parts)
claude "explain how you will implement Crank-Nicolson 
        before writing any code"

# Useful for reviewing what it built
claude "summarize all public interfaces you just created"
```

---

## The Meta-Advice

Claude Code is very good at **implementing a clear spec** and quite unreliable at **making architectural decisions under ambiguity**. Your job before each session is to eliminate ambiguity. The work you've already done — the design doc, the decisions above — is genuinely the most valuable input you can give it. Most people skip that step and then spend time arguing with generated code.

The CLAUDE.md + spec-per-feature pattern essentially turns Claude Code into a fast, tireless junior developer who follows specs well. That's exactly the right mental model.

 

---

## The Real Way to Load Context

**CLAUDE.md is the correct mechanism** — and it's automatic. Just place it in your project root and Claude Code reads it at the start of every session. No flag needed:

```bash
cd ~/QM
claude                    # CLAUDE.md is read automatically
```
 
 

## Actual Useful Flags

These are the commands worth using day-to-day:

```bash
# Start interactive session (reads CLAUDE.md automatically)
claude

# One-shot, non-interactive — good for scripting
claude -p "explain the Grid class interface"

# Continue the most recent session
claude --continue

# Resume a specific past session
claude --resume SESSION_ID

# Pipe content in
cat specs/01-grid.md | claude -p "implement this spec"
```

For adding instructions beyond CLAUDE.md, the system prompt flags are the right tool — `--append-system-prompt` is the safe choice since it preserves Claude Code's built-in capabilities while adding your requirements. But for your use case, CLAUDE.md alone is sufficient.

---

## Useful In-Session Slash Commands

These are worth knowing once you're inside a session:

```
/plan        — read-only mode, Claude proposes changes without making them
               (use this before any large implementation step)
/compact     — compresses conversation history when context gets large
/clear       — wipes history, good when switching to a different task
/rewind      — rolls back to a previous point; can revert code only 
               while keeping conversation
```

`/init` is also worth running once at the start — it scans your project and creates a starter CLAUDE.md file automatically.

---

## The Pattern That Actually Works

```bash
# 1. Go to your project root (where CLAUDE.md lives)
cd ~/projects/schrodinger-solver

# 2. Start session — CLAUDE.md loads automatically
claude

# 3. Before any big implementation, use /plan first
# (inside the session)
> /plan
> implement the Grid class per specs/01-grid.md

# 4. Review the plan, then approve
# 5. Use /compact when context gets long
```

The key correction: **CLAUDE.md + `claude`** is the right approach, not any flag. The file does the work.

# Eigenvalue Solver (Stationary Mode)

## Goal
Solve H ψ = E ψ and return the lowest k eigenstates and energies.
This is the stationary mode core — all outputs must be physically correct
and validated against analytic solutions.

## Units
Atomic units throughout: ħ = m_e = 1.
Document this in every docstring.

## Interface
```python
from dataclasses import dataclass
import numpy as np
from scipy.sparse import spmatrix

@dataclass
class EigenstateResult:
    energies: np.ndarray        # shape (k,), ascending order
    wavefunctions: np.ndarray   # shape (k, grid.n), each row is one ψ
    grid_x: np.ndarray          # shape (grid.n,) — x coordinates
    dx: float                   # grid spacing
    converged: bool             # did eigensolver converge?
    norm_errors: np.ndarray     # |1 - ||ψ_i||²| for each eigenstate

def solve_eigenstates(
    hamiltonian: spmatrix,
    grid_x: np.ndarray,
    dx: float,
    k: int = 5,
) -> EigenstateResult:
```

## Implementation details
- Use scipy.sparse.linalg.eigsh with which='SM' (smallest magnitude)
- Normalize each wavefunction: ψ_i /= sqrt(sum(|ψ_i|²) * dx)
- Sort by energy ascending
- Sign convention: make the first non-negligible element of each ψ positive
- Return converged=False and log a warning if eigsh raises ArpackNoConvergence

## Tests required

### Infinite square well
- Domain: x ∈ [0, L], L = 1.0, n = 500 points
- Analytic: E_n = n²π²/2L² in atomic units (n = 1, 2, 3, ...)
- Assert first 5 energies within 0.5% of analytic values

### Harmonic oscillator
- Domain: x ∈ [-8, 8], V(x) = x²/2, n = 500 points
- Analytic: E_n = n + 1/2 (n = 0, 1, 2, ...)
- Assert first 5 energies within 0.5% of analytic values

### Normalization
- ||ψ_i||² * dx == 1.0 within 1e-6 for all returned eigenstates

### Orthogonality
- |<ψ_i|ψ_j>| * dx < 1e-6 for all i ≠ j

### Ordering
- energies array is strictly ascending

## Do not implement
- 2D eigensolvers
- Degenerate subspace handling
- Any plotting or visualization



Good progress. Do these steps in order:

## 1. Verify the Tests Pass First

Before looking at anything else:

```bash
cd backend
pip install pytest numpy
pytest tests/test_grid.py -v
```

If tests fail — go back to Claude Code and say:
```
tests are failing, fix them:
[paste the error output]
```

Don't proceed until tests are green.

---

## 2. Review What Was Actually Built

```bash
cat backend/grid.py
```

Check specifically:
- Is the `Grid` class interface what you specified? (`x`, `dx`, `n`, `shape` properties)
- Is it dimension-agnostic? (no hardcoded 1D assumptions)
- Any `eval()` or bare `except:` clauses? (red flags)

If something is wrong, tell Claude Code precisely what to fix rather than asking it to redo everything.

---

## 3. Commit What Works

```bash
cd backend
git init          # if not already a git repo
git add grid.py tests/test_grid.py
git commit -m "feat: Grid abstraction with dimension-agnostic interface"
```

Commit working code before moving to the next feature. This gives you a safe rollback point and makes `/rewind` in Claude Code more useful.

---

## 4. Move to the Next Spec

Once Grid is green and committed, the natural next step is the **Hamiltonian builder** — it depends on Grid but nothing else yet. Write `specs/02-hamiltonian.md` before asking Claude to implement it:

```markdown
# Hamiltonian Builder

## Goal
Build a sparse 1D Hamiltonian matrix H = T + V
where T is the kinetic energy operator and V is the potential.

## Interface
build_hamiltonian(grid: Grid, potential: np.ndarray) -> scipy.sparse matrix
- grid: a Grid instance
- potential: array of V(x) values, shape (grid.n,)
- returns: sparse matrix of shape (grid.n, grid.n) in atomic units (ħ=m=1)

## Kinetic energy discretization
Use 3-point finite difference for second derivative:
T_ij = -1/2 * (ψ_{i+1} - 2ψ_i + ψ_{i-1}) / dx²
T is a tridiagonal sparse matrix (use scipy.sparse.diags)

## Tests required
- Matrix is symmetric: H == H.T within 1e-10
- Infinite square well ground state energy within 0.1% of π²/2L²
  (L = x_max - x_min, use eigsh to get lowest eigenvalue)
- Matrix shape is (grid.n, grid.n)

## Do not implement
- Time evolution
- Any eigenvalue solving beyond the test
```

Then in Claude Code:

```
read specs/02-hamiltonian.md and implement it.
write tests first, then implementation.
do not proceed past the tests without stopping.
```

---

## The Rhythm Going Forward

```
write spec → Claude implements → tests pass → review → commit → next spec
```

 



The specs to write in order are:

1. ✅ Grid
2. Hamiltonian builder (kinetic + potential)
3. Eigenvalue solver (stationary mode)
4. Crank-Nicolson stepper (time evolution)
5. FastAPI endpoints
6. React frontend scaffold
7. Wire frontend to backend

Don't let Claude jump ahead to step 5 while step 2 is untested. Each layer depends on the one below it being correct.
